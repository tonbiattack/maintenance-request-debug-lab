# 設計方針

この教材は、サービス数を増やさずに、実務で頻出する状態管理・HTTP契約・SQL・Transaction・DB制約を切り分ける練習を目的とします。BrowserからFlask APIへ直接接続し、PostgreSQLだけへ永続化します。

複数サービスの障害伝播やキャッシュを扱わない代わりに、非決定的なページング、タイムゾーン、二重更新、部分コミット、制約不足、フォーム状態残留を詳しく観測します。
